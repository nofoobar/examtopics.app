# This file is intentionally empty.
# Langfuse tracing is handled automatically by the langfuse.openai
# drop-in replacement in utils/openrouter.py.
# The LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_HOST env vars
# are read directly by the langfuse library — no manual client needed.
